name: Release

on:
  push:
    tags:
      - "v*"

env:
  CARGO_TERM_COLOR: always

jobs:
  validate-tag:
    name: Validate tag matches crate versions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Extract tag version
        id: tag
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
      - name: Check problem-details version
        run: |
          CRATE_VERSION=$(cargo metadata --no-deps --format-version=1 \
            | jq -r '.packages[] | select(.name == "problem-details") | .version')
          if [ "$CRATE_VERSION" != "${{ steps.tag.outputs.version }}" ]; then
            echo "::error::Tag v${{ steps.tag.outputs.version }} does not match problem-details version $CRATE_VERSION"
            exit 1
          fi
      - name: Check problem-details-axum version
        run: |
          CRATE_VERSION=$(cargo metadata --no-deps --format-version=1 \
            | jq -r '.packages[] | select(.name == "problem-details-axum") | .version')
          if [ "$CRATE_VERSION" != "${{ steps.tag.outputs.version }}" ]; then
            echo "::error::Tag v${{ steps.tag.outputs.version }} does not match problem-details-axum version $CRATE_VERSION"
            exit 1
          fi

  ci:
    name: CI checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --all -- --check
      - run: cargo clippy --all-targets --all-features -- -D warnings
      - run: cargo test --workspace --all-features
      - run: cargo doc --workspace --no-deps

  publish:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    needs: [validate-tag, ci]
    environment: crates-io
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Publish problem-details
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish -p problem-details --locked
      - name: Wait for crates.io index update
        run: sleep 30
      - name: Publish problem-details-axum
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish -p problem-details-axum --locked
